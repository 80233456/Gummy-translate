# Gummy 外教实时翻译：项目规划

更新日期：2026-09-08

当前版本：0.4.0-alpha03.2

目标设备：一加 13（arm64-v8a）

最低系统：Android 8.0（API 26）

## 1. 项目目标

打造一款面向真实外教课场景的轻量 Android 实时语音翻译工具。用户在手机端填写自己的阿里云百炼 API Key，即可通过麦克风完成英语识别、中文实时翻译、课堂记录与课后整理。

产品原则：

- 课堂中第一优先级是“最新翻译始终看得见”。
- 尽量采用 Android 原生组件，控制安装包、内存和维护成本。
- 保持纸质暖色视觉，减少长时间阅读疲劳。
- API Key 仅保存在应用私有空间，不写入源码、日志或安装包。
- 网络或服务异常时给出明确状态，不制造空记录或静默丢失内容。

## 2. 当前实现

### 2.1 实时课堂

- 使用 `gummy-realtime-v1` 完成英语识别与中文翻译。
- 通过官方 Android NUI SDK 获取 16 kHz 单声道 PCM 音频。
- 按 `sentence_id` 和 `sentence_end` 合并异步识别、翻译结果。
- 当前英文字幕最多显示四行、中文字幕最多显示五行，并自动显示最新文本。
- 实时小窗采用紧凑多行排版，增量文字以淡入上浮方式出现。
- 全局采用系统衬线字体，保持纸质阅读风格一致。
- 使用书页与双向声波构成的暖色品牌图标，并接入 Android 自适应启动图标。
- 首页强化品牌区、主操作层级、卡片阴影和底部导航选中状态。
- 为首页、课程记录、设置、课堂控制和记录管理补齐统一的原生矢量组件图标。
- 完整字幕进入历史列表后自动跟随底部。
- 用户主动上滑时暂停自动跟随，并显示半透明回到底部按钮。
- 支持暂停、继续、保存退出和不保存退出。
- 横竖屏切换不重建课堂会话，内存字幕和连接保持不变。
- 连接超过 12 秒或意外中断时最多自动重连三次，失败后提供手动重试。
- 暂停恢复时重置流式句子状态，避免字幕窗口停止更新。
- 课堂期间可选择保持屏幕常亮。

### 2.2 音频增强

- Android 原生环境降噪（NoiseSuppressor）。
- Android 原生回声消除（AcousticEchoCanceler）。
- 可选自动增益（AutomaticGainControl）。
- 应用内自适应 PCM 增益，支持原始、4× 和 8× 三档并自动防止削波。
- 轻声保护模式将断句等待延长至 1200ms，减少尾音被提前截断。
- 新增近距离、普通课堂、远距离/视频三种声音预设；弱音模式支持 12× 上限、1 秒句尾增益保持和 1800ms 断句等待。
- 根据设备能力自动禁用不支持的效果。
- 一加 13 当前实测支持降噪和回声消除，不支持系统自动增益。

### 2.3 课程记录

- 使用 SQLite 保存课程、时间和双语字幕。
- 首页最近课程可直接进入详情。
- 支持查看、重命名、复制全文、系统分享和删除课程。
- 支持清空全部课程记录。
- 时长按秒、分秒或小时显示。
- 无有效字幕的会话不保存为空课程。

### 2.4 设置

- 填写、显示、保存并在线测试 API Key。
- 设置课堂屏幕常亮。
- 设置断句等待：400 / 600 / 800 / 1200 ms。
- 设置实时字幕字号：紧凑 / 标准 / 大字。
- 独立控制降噪、回声消除和自动增益。

## 3. 技术结构

- UI：Android 原生 XML View。
- 语言：Java。
- 构建：Gradle Android Application Plugin。
- 实时语音：Alibaba NUI Android SDK。
- 服务：阿里云百炼 Gummy WebSocket API。
- 本地数据：SQLiteOpenHelper。
- 网络测试：OkHttp WebSocket。
- 当前 ABI：`arm64-v8a`。

主要模块：

- `MainActivity`：首页和最近课程。
- `LiveActivity`：录音、实时翻译、字幕状态与课堂控制。
- `HistoryActivity`：课程列表和记录管理。
- `SessionDetailActivity`：课程详情、重命名、复制、分享和删除。
- `SettingsActivity`：Key、显示、断句和音频增强设置。
- `AppStorage`：课程与字幕持久化。

## 4. 第二轮计划：稳定性与课堂体验

当前进度：已建立课堂前台服务、常驻状态通知、CPU 唤醒保护与服务端有效课堂计时；实时小窗保留 Gummy 原译，历史窗口已接入 Qwen-MT Flash 异步优化。下一步将把 NUI、AudioRecord 和字幕状态完整迁入服务。

### P0：长时间课堂可靠性

- 增加断网检测和有限次数自动重连。
- 重连时保留已经完成的字幕，清理未完成句子的状态。
- 区分网络中断、Key 失效、额度不足、麦克风异常和服务异常。
- 增加 30、60、90 分钟连续运行测试。
- 检查暂停、来电、切换应用、锁屏和音频设备变化后的恢复行为。
- 防止重复回调、重复字幕和结束课堂时遗漏最后一句。

完成标准：连续运行 60 分钟无崩溃；短暂断网恢复后可以继续翻译；已经保存的字幕不丢失。

### P1：音频与识别体验

- 在真实教室、手机外放、耳机和安静房间分别测试音频效果。
- 根据实测结果确定降噪、回声消除的最佳默认值。
- 显示当前有效的音频增强状态，而非仅显示设置开关。
- 增加蓝牙耳机和有线耳机输入识别。
- 评估麦克风电平提示，帮助用户判断距离和音量。
- 评估是否需要“安静环境 / 普通教室 / 嘈杂教室”预设。

完成标准：外放场景明显减少回声重复识别；降噪不会过度吞掉远距离人声。

### P1：交互与视觉完善

- 统一按钮按压、弹窗、页面进出和列表更新动画。
- 优化横屏、分屏、字体放大和不同屏幕尺寸布局。
- 为暂停、重连、保存和错误状态建立统一视觉规范。
- 检查无障碍标签、触控区域、文字对比度和系统深色模式行为。
- 增加首次使用引导，解释 Key、麦克风权限和手机摆放方式。

完成标准：主要操作单手可达；系统字体调至 130% 时无严重遮挡；横竖屏切换不丢课堂状态。

## 5. 第三轮计划：课后整理与交付

### 课程资料

- 导出 TXT、Markdown 和带时间信息的字幕文件。
- 支持按课程名称和日期搜索。
- 支持批量选择、删除和分享。
- 支持为课程添加备注或标签。
- 评估中英文分栏导出和重点句收藏。

### 数据安全

- 为 API Key 增加 Android Keystore 加密存储。
- 设置页仅显示脱敏 Key，显示完整内容前进行明确操作。
- 增加本地数据库备份与恢复。
- 明确清除数据、卸载应用和更换设备时的数据行为。
- 发布前更换曾在聊天或测试环境中暴露过的 API Key。

### 可交付构建

- 建立正式 release 签名，不使用 debug 签名交付。
- 增加版本更新记录和应用内版本信息。
- 生成可复现的 release APK，并保存 SHA-256。
- 对安装、首次授权、设置 Key、开始课堂、暂停恢复、退出保存、历史管理进行回归测试。
- 准备隐私说明：录音用途、云端处理范围、本地数据位置和删除方式。

完成标准：全新设备可独立完成安装和配置；release 包签名稳定；核心回归用例全部通过。

## 6. 风险与决策

### Gummy 服务生命周期

移动 SDK 示例曾将 Gummy 标为 Deprecated，而公开模型接口目前仍可使用。项目需要把语音服务封装边界保持清晰，以便未来替换为其他实时翻译模型或直接使用 WebSocket 实现。

应对方案：

- 不在 UI 和数据库层依赖 SDK 特有对象。
- 保留统一的“转写增量、翻译增量、句子完成、错误、连接状态”事件模型。
- 在正式交付前再次确认模型可用地域、计费和下线计划。

### 设备音频差异

Android 音频效果由设备厂商实现，同一开关在不同手机上的效果可能不同。

应对方案：

- 运行时检测能力。
- 所有效果允许单独关闭。
- 以真实录音测试决定默认值，不假设所有设备表现一致。

### API Key 安全

直接在客户端保存长期 Key 适合个人自用，不适合公开发布。

应对方案：

- 个人交付阶段使用 Keystore 加密。
- 若公开分发，改为服务端签发短期凭证并增加配额控制。

## 7. 测试清单

每次发布至少验证：

- 全新安装、覆盖安装和数据库升级。
- 麦克风首次授权、拒绝授权和重新授权。
- 短句、连续长句、快速讲话与长时间静音。
- 中英文结果到达顺序不同的情况。
- 暂停后继续至少三次。
- 用户上滑历史、点击回到底部、继续自动跟随。
- 保存退出、不保存退出和系统返回键。
- 无网络、弱网、切换 Wi-Fi/移动网络。
- 降噪与回声消除分别开启和关闭。
- 课程重命名、复制、分享、删除和清空。
- 不足一分钟、超过一分钟和超过一小时的时长显示。
- 应用切后台、锁屏、旋转屏幕和进程被系统回收。

## 8. 近期执行顺序

1. 完成长时间课堂和断网重连机制。
2. 在一加 13 上完成四类真实音频场景对比测试。
3. 修复生命周期与横竖屏状态保持。
4. 完成课程搜索、导出和备份。
5. 加密 API Key，建立正式 release 签名。
6. 完成全量回归后发布 1.0.0。

## 9. 版本里程碑

- 0.3.x：稳定性、音频增强和设置完善。
- 0.4.x：网络重连、生命周期和长时间课堂。
- 0.5.x：课程搜索、导出、备份与课后整理。
- 0.9.x：安全加固、正式签名、隐私说明和发布候选版。
- 1.0.0：完成核心回归并形成可交付版本。
